@model MemoryGame.Board

<h2>Memory Game Board</h2>
<table>
    @for (int i = 0; i < Model.cells.Length; i++)
    {
                <tr>
            @for (int j = 0; j < Model.cells[i].Length; j++)
            {
                            <td>
                    <img src="C:\Mentorship\MemoryGameMVC\images\cards\10_of_clubs.png">
                            </td>
            }
                </tr>
    }
    </table>
    